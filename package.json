{
  "name": "project-pulse-root",
  "version": "1.0.0",
  "private": true,
  "description": "Orquestrador de comandos para o Project Pulse (Frontend & Backend)",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "npx -y concurrently -k -p \"[{name}]\" -n \"Backend,Frontend\" -c \"yellow,cyan\" \"npm run dev:backend\" \"npm run dev:frontend\""
  }
}
